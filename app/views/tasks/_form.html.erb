<%= simple_form_for task do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.submit t('buttons.submit'), class: "btn btn-primary form-control mt-xs-20" %>
  <%= link_to t('buttons.cancel'), tasks_path, class: "btn btn-default form-control mt-xs-20" %>
<% end %>